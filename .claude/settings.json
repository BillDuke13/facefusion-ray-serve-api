{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; [ -n \"$f\" ] || exit 0; case \"$f\" in *.py) command -v ruff >/dev/null 2>&1 || exit 0; ruff format \"$f\" >/dev/null 2>&1; ruff check --fix \"$f\" >/dev/null 2>&1 ;; esac; } 2>/dev/null || true",
            "statusMessage": "Formatting with ruff..."
          }
        ]
      }
    ]
  }
}
